# 智枢（Zhishu Agent）v1.0.54 —— 各功能模块复校报告（双端）

> 复校时间：2026-08-30
> 复校对象：v1.0.54 部署后的**本地实例**与**远程实例**双端交叉校验
> 复校目的：确认 1.0.54 部署（含 F1 `learning-graph` 路由修复）后各功能模块无回归，并与《功能测试报告_v1.0.54》基线比对

---

## 一、复校范围与双端环境

| 项 | 本地实例 | 远程实例 |
|---|---|---|
| 地址 | `localhost:8080` | `152.136.33.55:8090`（容器内监听 8080） |
| 容器 | `zsagent`（镜像标签 1.0.53） | `zsagent`（镜像标签 **1.0.54**） |
| 代码来源 | bind-mount 热更（仓库最新代码） | 镜像内固化（tar 同步后 `--no-cache` 构建） |
| `/health` | `{"version":"1.0.54"}` | `{"version":"1.0.54"}` |
| 环境约束 | ENV-LIMITED（无 provider key、`outbound_allow:false`） | 同左 |

**说明**：本地容器镜像标签虽为 1.0.53，但源码为 bind-mount 挂载，实际运行代码为 1.0.54（`/health` 已证实）。因此双端代码一致，具备交叉校验条件。
本地容器 `tests/` 与本地仓库 `backend/tests/` 已 md5 校验一致（`9ec9ca6f58a8...`），测试资产同步。

---

## 二、HTTP 模块链路校验（94 项 × 双端）

使用同一份 harness（`http_test.py`，stdlib urllib，A~R 共 19 组模块）。远程实例将脚本送入容器内执行（容器内 app 监听 8080，与 harness `BASE` 匹配）。

### 2.1 总体结果

| 端 | PASS | FAIL | ENV-LIMITED | WARN | TOTAL |
|---|---|---|---|---|---|
| 本地 | **92** | 0 | 2 | 0 | 94 |
| 远程 | **92** | 0 | 2 | 0 | 94 |
| 报告基线 | 92 | 0 | 2 | 0 | 94 |

✅ 双端结果与《功能测试报告_v1.0.54》基线**完全一致**，无回归。

### 2.2 按模块明细（双端逐项一致）

| 模块 | 覆盖内容 | PASS | 受限 |
|---|---|---|---|
| A-health | 健康检查 | 1 | 0 |
| A-auth | 鉴权基础 | 2 | 0 |
| B-auth | 登录/令牌/登出/吊销 | 4 | 0 |
| C-rbac | 角色权限矩阵 | 5 | 0 |
| D-users | 用户管理 | 8 | 0 |
| E-prov | Provider/模型管理 | 7 | 0 |
| F-know | 知识库 | 7 | 0 |
| G-conv | 会话管理 | 5 | 0 |
| **H-skill** | **技能（含 learning-graph / curator / 导入导出）** | **11** | 0 |
| I-plug | 插件 | 6 | 0 |
| J-mcp | MCP | 7 | 0 |
| K-mem | 记忆 | 5 | 0 |
| L-agent | 子智能体 | 7 | 0 |
| M-cron | 定时任务 | 6 | 0 |
| N-set | 设置 | 3 | 0 |
| O-admin | 管理/审计/脱敏 | 4 | 0 |
| P-tools | 工具清单 | 1 | 0 |
| Q-media | 媒体鉴权门 | 1 | 0 |
| R-llm | LLM/兼容网关 | 2 | **2** |
| **合计** | | **92** | **2** |

**F1 修复复核**：双端 `GET /api/v1/skills/learning-graph` 均返回 **200**（此前被 `/skills/{name}` 遮蔽为 404）；未知技能名 `does-not-exist-xyz` 返回 **404**（路由隔离正确）。

**2 项 ENV-LIMITED**（非缺陷，环境所致）：
- `POST /v1/chat/completions`（无 provider key）
- `POST /api/v1/chat`（无 provider key）

---

## 三、单元测试回归（双端）

### 3.1 测试文件分类

因部分测试文件为「脚本式」（module-level `sys.exit`，无法被 pytest 收集），先用 AST 精确分类：
- **脚本式 12 个**（单独 `python` 执行）
- **pytest 可收集 28 个**（其中 25 个 `test_*.py` 被实际收集）

### 3.2 pytest 可收集套件

| 端 | 结果 |
|---|---|
| 本地 | **149 passed / 1 failed** |
| 远程 | **149 passed / 1 failed** |

唯一失败项：`test_override_stamp_delegate_memory.py::test_delegate_passes_shared_manager_readonly`
- 单独运行该文件 → **11 passed（全通过）**
- 判定：跨文件**全局 ctx 单例污染**（见 §四）

### 3.3 脚本式测试（12 个文件，双端各 12/12 `exit=0`）

| 文件 | 结果（本地/远程一致） |
|---|---|
| smoke_skill_alignment.py | 9 通过 / 0 失败 |
| test_antirunaway_breaker.py | 42 通过 / 0 失败 |
| test_chat_http_e2e.py | ALL PASSED |
| test_codeblock_fallback.py | 13 通过 / 0 失败 |
| test_docproc_e2e.py | PASS 9 / FAIL 0 |
| test_download_guard.py | 33 通过 / 0 失败 |
| test_multiagent_e2e.py | ALL E2E PASSED |
| test_office_zip_dispatch.py | 28 通过 / 0 失败 |
| test_openai_gateway.py | ALL GATEWAY PASSED |
| test_p0_compaction_fence.py | 11 通过 / 0 失败 |
| test_p1_background_review.py | 20 通过 / 0 失败 |
| test_repair_tool_args.py | ALL REPAIR PASSED |

✅ 双端**无任何差异**。

---

## 四、发现项与判定（2 项，均为**测试基础设施**问题，非业务回归）

### F2｜async 测试运行器未启用（影响 11 项，已定位解法）
- 现象：未指定运行器时，11 个 async 测试报 `async def functions are not natively supported` 而失败。
- 根因：两端均已安装 `pytest-asyncio 1.4.0`，但默认 `asyncio_mode=strict`，而这些异步测试未加 `@pytest.mark.asyncio` 标记。
- 解法：运行时加 `-o asyncio_mode=auto` 后，**11 项全部通过**（失败数 11 → 1）。
- 建议：在项目内固化 `pytest.ini`/`pyproject.toml` 的 `asyncio_mode = auto`，避免依赖命令行参数。

### F3｜全局 ctx 单例污染（影响 1 项）
- 现象：`test_override_stamp_delegate_memory.py` 全量运行时 1 项失败（`None is not <FakeMemoryManager>`），单独运行则 **11 全通过**。
- 根因：`init_ctx()` 为全局单例；全量运行时其它测试文件的 `cfg` 已占用该单例，导致本用例的 `data_dir` 指向已被删除的临时目录 → `get_agent_meta()` 返回空 → `_run_delegate` 在构造子智能体前提前 return → 捕获参数为空。
- 判定：**夹具隔离问题，非业务代码回归**。业务逻辑本身正确——`agent.py:2246` 确实传递了 `memory_manager=self.memory_manager, memory_read_only=True`；`can_view` 对无主资源返回 True；`is_enabled` 亦不拦截。
- 建议：为该用例在 setUp 中显式重置/隔离 ctx（或改用独立进程运行）。

---

## 五、各模块校验结论

| 模块域 | 结论 | 备注 |
|---|---|---|
| 健康检查 / 鉴权 / RBAC | ✅ 通过 | A、B、C 组全绿 |
| 用户 / Provider / 模型 | ✅ 通过 | D、E 组 |
| 知识库 / 会话 | ✅ 通过 | F、G 组 |
| **技能（含学习图谱）** | ✅ **通过（F1 已修复并双端复核）** | learning-graph 200、curator、导入导出 |
| 插件 / MCP / 子智能体 | ✅ 通过 | I、J、L 组 |
| 记忆（含向量/FTS） | ✅ 通过 | K 组 + 单测 |
| 定时任务 / 设置 / 管理审计 / 脱敏 | ✅ 通过 | M、N、O 组 |
| 工具 / 媒体鉴权 | ✅ 通过 | P、Q 组 |
| LLM / OpenAI 兼容网关 | ⚠️ 2 项环境受限 | 需补全 provider key 后复测 |
| 文档处理 / 反空转 / 背景反思 / 网关 | ✅ 通过 | 脚本式专项测试全绿 |

---

## 六、结论

1. **v1.0.54 部署后各功能模块无回归**：HTTP 94 项双端各 92 通过 / 0 失败 / 2 环境受限，与基线一致。
2. **F1 修复双端确认生效**：`GET /api/v1/skills/learning-graph` 两端均 200。
3. **本地与远程零差异**：HTTP 明细、pytest 结果、脚本式测试逐项一致，说明镜像构建产物与仓库源码同步正确。
4. **发现 2 项测试基础设施问题（F2/F3）**，均非业务缺陷，已给出明确解法与固化建议。

### 后续建议
1. 固化测试依赖与配置：将 `pytest` + `pytest-asyncio` 写入测试依赖，并在配置中固定 `asyncio_mode = auto`（解决 F2）。
2. 修复 F3 夹具隔离：为该类用例重置全局 ctx，使全量套件可稳定全绿。
3. 补全 Provider Key 后复测 2 项 ENV-LIMITED（chat 推理链路）。
