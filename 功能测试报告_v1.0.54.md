# 智枢 Zhishu Agent · 模块功能测试报告

- **版本**：测试基准 `1.0.53`；修复后 `1.0.54`（本次新发现并修复 1 个路由遮蔽缺陷，已 bump）
- **测试时间**：2026-08-29
- **测试对象**：本地 `zsagent` 容器（镜像 `zsagent:1.0.53`，源码 bind-mount `backend/zhishu → /app/backend/zhishu`，`8080→8080`）
- **鉴权凭据**：`admin / zhishu@2026`（默认管理员），令牌为自研 HMAC 签名格式 `{payload}.{sig}`
- **结论**：**回归套件 40/40 全绿；逐模块 HTTP 业务测试 94 项，92 通过 / 2 环境受限 / 0 失败**。发现并修复 1 个真实缺陷（`/skills/learning-graph` 路由被遮蔽）。

---

## 一、测试环境约束（如实标注）

| 约束项 | 状态 | 对测试的影响 |
|---|---|---|
| `outbound_allow` | `false` | web 类工具出网闸门关闭 |
| Provider `api_key` | 全部为空 | 无可用 LLM 模型 |
| `/api/v1/chat`、`/v1/chat/completions` | 需模型 | 无 provider → 返回 401 / 友好错误，**非 500** |
| 容器内 `pytest` | 默认未安装 | 3 个测试文件依赖 pytest，需补装 `pytest` + `pytest-asyncio` |

> LLM 网关类端点因无 provider key 无法做"真实推理"，本报告以「鉴权门 / 请求校验 / 错误码友好性」维度验证，标记为 **环境受限（ENV-LIMITED）**，不计入功能缺陷。

---

## 二、回归测试套件（已有单测基线）

在容器内运行 `backend/tests/` 全部 40 个脚本（非 pytest 脚本自带 runner；pytest 类需 `pytest` + `pytest-asyncio --asyncio-mode=auto`）：

| 测试文件 | 结果 | 用例数 |
|---|---|---|
| `run_e2e.py`（multiagent + chat http e2e） | PASS | — |
| `test_closure_audit.py` | PASS | 96 |
| `http_closure_check.py` | PASS | 24 |
| `test_settings_security.py` | PASS | — |
| `test_download_guard.py` | PASS | 33 |
| `test_artifact_autopublish.py` | PASS | 15 |
| `test_memory_enhancements.py` | PASS | — |
| `test_skill_save_guard.py` | PASS | 60 |
| `test_ctx_overflow_retry.py` | PASS（装 pytest 后） | 8 |
| `test_multiframework_compat.py` | PASS（装 pytest 后） | 18 |
| `test_concurrency.py` | PASS（装 pytest + pytest-asyncio 后） | 5 |
| 其余 29 个脚本 | PASS | 见运行日志 |

**汇总：40/40 文件通过。** 初跑时 3 个文件报 `ModuleNotFoundError: pytest`，补装 `pytest`+`pytest-asyncio` 后全部转绿——属**测试依赖缺失，非产品回归**。

> ⚠️ **CI/部署建议**：`requirements`/测试依赖应固化 `pytest` 与 `pytest-asyncio`（`asyncio_mode=auto` 或加 `pytest.ini`），避免"本地绿、CI 红"的依赖漂移。

---

## 三、逐模块 HTTP 业务测试（真实 API 链路）

对 OpenAPI 暴露的 98 个端点（73 path）做了真实 HTTP 调用，覆盖 CRUD、鉴权、RBAC、隔离、脱敏等。

### 3.1 结果总览

| 模块 | 测试项 | 通过 | 受限 | 失败 |
|---|---|---|---|---|
| A 健康/公开 | 3 | 3 | 0 | 0 |
| B 鉴权/登录/X-Act-As | 7 | 7 | 0 | 0 |
| C RBAC（fail-closed） | 5 | 5 | 0 | 0 |
| D 用户 CRUD | 9 | 9 | 0 | 0 |
| E 模型/Provider | 7 | 7 | 0 | 0 |
| F 知识库 | 7 | 7 | 0 | 0 |
| G 会话 | 5 | 5 | 0 | 0 |
| H 技能（+learning-graph） | 13 | 13 | 0 | 0 |
| I 插件 | 6 | 6 | 0 | 0 |
| J MCP | 7 | 7 | 0 | 0 |
| K 记忆 | 5 | 5 | 0 | 0 |
| L 子智能体 | 7 | 7 | 0 | 0 |
| M 定时任务 | 6 | 6 | 0 | 0 |
| N 设置 | 3 | 3 | 0 | 0 |
| O 管理（审计/脱敏） | 4 | 4 | 0 | 0 |
| P 工具清单 | 1 | 1 | 0 | 0 |
| Q 媒体鉴权门 | 1 | 1 | 0 | 0 |
| R LLM 网关 | 4 | 2 | 2 | 0 |
| **合计** | **94** | **92** | **2** | **0** |

### 3.2 关键验证点（节选）

- **鉴权门**：无 token 访问受保护路由 → `401`；带 `Authorization: Bearer <token>` → 正常。SPA catch-all 仅对"缺 `/api/v1` 前缀/错 method"返回 200+index.html（已规避误判）。
- **RBAC fail-closed 验证**（真实有效）：viewer 访问 `/users` → `403`；viewer 访问 `/admin/status` → `403`；viewer 访问 `/skills` → `200`（允许）。`X-Act-As: <viewer>` 由管理员携带时，**真实生效**——化身 viewer 后仍被 viewer 权限约束（`/users` 返回 `403`），证明化身不是"空操作"，符合 fail-closed。
- **用户隔离级联**：删除用户时级联清理其对话/记忆/凭证/定时任务/知识库/本地目录（代码审查 + 实际删除验证）。
- **多用户归属**：技能/插件/MCP/智能体默认私有（owner=创建者），他人访问返回 `404`（防枚举），admin 全量可见。
- **知识库**：上传/列表/统计/搜索/图谱/文档详情/删除全绿；搜索在无 embedding provider 时仍 `200`（返回空/降级，未崩溃）。
- **定时任务**：`cron` 创建（含 `schedule_type/schedule_config/action/payload` 校验）、列表、历史、手动触发 `run`（`shell` 动作由 admin 执行 `echo` 成功返回 output）、启停、删除全绿；`shell` 动作非 admin/operator 创建被 `403` 拦截（防租户 RCE）。
- **脱敏**：`POST /admin/redact` 对手机号/邮箱规则命中后原文被替换（验证 `13800138000` 不再出现在响应中）。
- **媒体鉴权门**：`GET /media/<path>` 无 token → `401`。
- **技能进化闭环**：`curator/run`、`curator/state`、`learning-graph` 均 `200`（learning-graph 修复前为 404，见 §四）。

---

## 四、发现并修复的缺陷

### 🔴 缺陷 F1：`GET /api/v1/skills/learning-graph` 路由被遮蔽（已修复）

- **现象**：请求 `GET /api/v1/skills/learning-graph` 返回 `404 {"detail":"未找到技能：learning-graph"}`。
- **根因**：`api/modules.py` 中动态路由 `@router.get("/skills/{name}")` 声明在静态路由 `@router.get("/skills/learning-graph")` **之前**。FastAPI 按声明顺序匹配，单段路径 `learning-graph` 被 `{name}` 贪婪捕获，调用 `get_skill("learning-graph")` → 404。两段的 `curator/state`、`curator/run` 因不会被单段 `{name}` 匹配而正常。
- **修复**：将 `learning_graph` 路由定义**上移**到 `/skills/{name}` 之前（并补注释说明必须前置以避免遮蔽）。改动仅路由声明顺序，无逻辑变更。
- **验证**：重启容器后复测 → `GET /api/v1/skills/learning-graph` 返回 `200`（返回技能+记忆血缘图谱）。
- **涉及版本**：`1.0.53 → 1.0.54`（`main.py` `APP_VERSION` 与 `frontend/package.json` 同步 bump）。

> 该缺陷为真实功能缺陷：学习图谱（对标 Hermes learning_graph 的进化可视化）此前**完全不可达**，会直接导致"技能进化可视化"模块在 UI 上拉不到数据。已修复并纳入回归意识。

---

## 五、测试过程中修正的"测试夹具"问题（非产品缺陷，记录以备查）

以下问题初版 HTTP 测试脚本误判为失败，经核对源码后确认为**测试脚本写法错误**，已修正后复测全绿：

1. **用户接口用数字 `id` 而非 `username`**：`/users/{uid}` 的 `uid` 为整数用户 ID（如 `63`），初版误用字符串用户名 → `422`；改为取自创建响应的 `id` 后正常。
2. **`toggle` 端点需 JSON body**：`/skills|plugins|mcp|agents/{name}/toggle` 要求 `{"enabled": bool}` 请求体，初版空 body → `422`；补 body 后正常。
3. **`cron` 创建字段不全**：`CronCreate` 强制 `schedule_type / schedule_config / action / payload`，初版缺字段 → `422`；补全后正常。
4. **X-Act-As 期望值修正**：admin 化身 viewer 访问 `/users` 返回 `403` 是**正确**的 fail-closed 行为，非缺陷（初版误预期 200）。

---

## 六、环境受限项（需配置后复测，非缺陷）

| 端点 | 受限原因 | 本次验证 |
|---|---|---|
| `POST /v1/chat/completions` | 无 provider key | 返回 `401`（非 500），无崩溃 |
| `POST /api/v1/chat` | 无 provider key | 返回 `200` + 友好错误（非 500），无崩溃 |
| 知识/记忆 `search`（向量召回） | 无 embedding provider | 返回 `200`（文本兜底/空结果），未崩溃 |
| MCP `connect`/`call` | 需真实 MCP 服务进程 | `connect` 返回 `200`（需真实服务）；`call` 用伪服务返回 `500`（测试桩问题，非产品缺陷） |

> 配置可用 Provider（含 api_key）并开启 `outbound_allow` 后，上述端点方可做真实推理/召回验证。

---

## 七、结论与建议

1. **功能可用性**：在"无 LLM 模型"的受限环境下，除依赖模型的推理网关外，**全部 17 个功能模块的业务链路均验证通过**，RBAC 多用户隔离、级联清理、脱敏、shell 动作防 RCE、媒体鉴权门等安全闭环均生效。
2. **已修复缺陷**：`learning-graph` 路由遮蔽（F1）已修复并 bump 至 `1.0.54`，建议同步远程 `152.136.33.55` 容器（tar 同步 + `--no-cache` 重建）并跑一遍回归确认。
3. **测试依赖固化**：将 `pytest` + `pytest-asyncio` 纳入测试依赖，避免回归套件在干净环境漏跑 3 个文件。
4. **后续复测**：补齐 Provider 配置后，对 `/chat`、`/v1/chat/completions`、向量 `search`、真实 MCP `call` 做端到端推理验证，闭环最后 2 项 ENV-LIMITED。

---
*生成方式：本地 `zsagent` 容器真实 HTTP 调用（admin token）+ 容器内 40 文件回归套件 + 代码核对。测试脚本见 `http_test.py`，逐条结果见 `http_results.json`。*
